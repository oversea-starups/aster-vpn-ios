import Foundation

public struct AppConstants {
    public static let appGroupName = "group.com.astervpn.shared"
    
    // Config keys used in UserDefaults or JSON files
    public struct Keys {
        public static let selectedNodeId = "selected_node_id"
        public static let connectionMode = "connection_mode"
    }
}
