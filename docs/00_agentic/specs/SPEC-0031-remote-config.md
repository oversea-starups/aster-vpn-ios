# SPEC-0031 — Remote Config 参数清单

## 目的
无需发版调整文案、试用、节点策略。

## 参数
- paywall_title (string)
- paywall_subtitle (string)
- cta_text_yearly (string)
- demo_duration_seconds (int, default 60)
- show_onboarding (bool, default true)
- enable_trial (bool, default true)
- enable_demo (bool, default true)
- pro_locations_enabled (bool)
